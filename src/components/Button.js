import React from 'react';

const Button = () => {
    return (
        <div>
            <input className="inputBtn " type="submit" value="Generate" />
        </div>
    );
};

export default Button;